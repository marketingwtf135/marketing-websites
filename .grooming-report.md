# Grooming report — 2026-04-30

## Summary

Second pass over the AXEVIL site. The user delivered 24 explicit feedback items via Agentation;
this pass executes all 24 against the new Figma file (NvXqEDqp86ZUo8KpZrD0fn). Figma metadata
access was partially functional this session (some node IDs resolved to screenshots, root frame
metadata timed out / returned invalid for the original `34:497`), so the new node IDs supplied
in the feedback (`64:4357`, `64:4317`, `64:4216`, `64:4139`, `64:290`, `64:247`) became the
ground truth wherever they could be pulled.

- Total feedback items: 24
- Fixed in this pass: 24
- Deferred: 0

---

## Block 01 — Hero

### Fixed
- **#1 Nav chevron icon**: replaced `arrow-down.svg` with a clean 16×16 viewBox + 1.5 stroke. Old SVG used `viewBox="0 0 10.3334 5.00001"` with `preserveAspectRatio="none"`, which stretched the V vertically when rendered at 16×16. New SVG uses a square 16×16 viewBox so the chevron renders to design proportions in every nav button.
- **#2 CtaButton 64px height**: shared `CtaButton` switched from `pb-4` + `paddingTop: '13px'` (variable height ≈ 53–60px depending on content) to `h-16` (locked 64px). Affects Blocks 01, 06, 07, 12 simultaneously since the component is shared.
- **#3 Hero illustration anchor**: removed the 1597×869 absolute pixel-positioned wrap (`left: 822px, top: 211px`). Now `position: absolute; top: 0; right: 0; width: 50%; height: 100%; overflow: clip`. The image fills the right 50% of the section with `width: 160%; max-width: none` so the dashboard remains visible while shadows fade out via a left+bottom gradient mask.

---

## Block 02 — Partner logos

### Fixed
- **#4 Card stroke color**: `border border-white/30 hover:border-white` swapped for `border border-white/15` with no hover state. Matches the Figma 64:290 macro (very subtle, non-interactive border).
- **#5 H2 vertical clip**: removed redundant `leading-none tracking-[-0.02em]` (already in `text-h2` token) and added `pb-2` so descenders ('g' in "Heading") render below the line-height: 1 baseline without clipping.

### Side cleanups
- Card hover translate + transition removed (no hover state per Figma).

---

## Block 03 — Cards

### Fixed
- **#6 Section centers on 1440 content**: `<section className="relative w-full bg-page-bg overflow-clip" style={{height: '918px'}}>` rewrapped to `<section><div className="relative mx-auto w-full max-w-content overflow-clip" style={{height: '918px'}}>`. All 4 cards re-positioned by subtracting 240px from canvas-x (240→0, 595→355, 950→710, 1305→1065). The section now renders at 1440px max-width centered, regardless of viewport.
- **#7 Card gradient stroke**: every card's solid `border-solid border-white` replaced with the same gradient-border trick used by `LogoCard`: `linear-gradient(180deg, rgba(255,255,255,0.4) 0%, rgba(255,255,255,0) 100%)` painted on `border-box`, with a `var(--page-bg)` fill on `padding-box`. Matches Figma 64:4216 (white at top, fading down).
- Hover translate + ellipse-shift transitions on Card 04 removed (per macro static cards).

---

## Block 04 — Two products

### Fixed
- **#8 Connector removed**: the `ill-image-one-platform.png` overflow connector below the cards (1440×200) is gone. Section ends at the cards now.
- **#9 Left card phone**: replaced bezel + `mobile-app-image-02` composite (which stretched the 327×502 screen image to 227×492, distorting aspect from 0.651 to 0.461) with a single `mobile-app-sbs.png` at the correct phone aspect 0.489. Anchored at `right: 0, bottom: 0; width: 223px; height: 456px`.
- **#10 Right card phone (was iPad)**: removed the iPad bezel + landscape laptop-app-image-02 composite. New phone: `iphone-bezel.png` + `mobile-app-image-01.png` (Pro mobile content), positioned at `left: 480px, top: 110px; width: 230px; height: 430px` per the user spec. Phone bottom-right meets card bottom-right.

---

## Block 05 — Mobile

### Fixed
- **#11 Section 1440×1080 with 200px gap**: outer wrapper changed from `padding: '0 240px'` (which made the section grow proportionally on viewports wider than 1920) to `flex justify-center; padding: '100px 0'`. Inner section is now `w-full max-w-content` — locked to 1440px max width regardless of viewport. 100px top + 100px bottom gives 200px between Block04→05 and 05→06.
- **#12 Info card right anchor**: heading + info card were inside a `flex items-start justify-between` row at fixed `width: 1320px`. Replaced with two independent absolute elements: heading at `left: 60px, top: 60px`, info card at `right: 60px, top: 60px`. Info card now snaps to the section's right edge regardless of section width.

### Side cleanups
- `border-[3px]` and `border-[8px]` arbitrary values replaced with `border-3` / `border-8` (new entries in `tailwind.config.ts > borderWidth`).

---

## Block 06 — Tablet / Desktop switch

### Fixed
- **#13 Desktop interface crop**: removed the nested `border-2 border-surface-3` outer frame (1436×862) + inner `border-border-subtle` frame (1400×799) which used `object-cover` to fill, producing a 1.913→1.752 aspect crop on the 1440×753 native PNG. New layout is the image at native size (`width: 1440px, height: 753px`, `block w-full h-auto`) inside a single rounded-card-lg frame with the same fade-bottom overlay. No cropping.
- **#14 Feature item left-borders**: changed from `border-l border-white` (full-strength) to `border-l border-white/30` to match the macro's softer 30% stroke.
- **#15 CTA button 64px**: handled by the shared `CtaButton` fix (#2).

---

## Block 07 — Web App

### Fixed
- **#16 App illustration size**: `<img>` was hard-coded to `width: 978, height: 520`. Updated to `width={1055} height={788}` matching the spec, and switched to `w-full h-auto` with `maxWidth: 1055px` so it scales down proportionally on viewports narrower than 1055px. Centered via the existing `flex items-center` parent.
- **#17 CTA button 64px**: handled by the shared `CtaButton` fix (#2).

---

## Block 08 — Legal / Map

### Fixed
- **#18 Map width 742px max + corner anchors**: map image was 1440×620 spanning full canvas (with `width: 1440px`). Now wrapped in a 742px-max centered container (`left: 50%; width: 742px; transform: translateX(-50%)`) so the map renders at 742×547 max, scaling down on narrow viewports. The two info cards moved inside this container — Favorable Laws at `left: 0; top: 180px`, VC Standard at `right: 0; top: 120px`. Both now snap to the corners of the 742px map area rather than floating freely on the canvas.

---

## Block 11 — Second logos row

### Fixed
- **#19 LogoCard stroke + remove hover**: shared `LogoCard` had `border-white/30 hover:border-white` with a 180ms colour transition. Updated to `border-white/15` and removed the hover state entirely. Matches Figma 64:290 (static, low-contrast border, no hover).

---

## Block 12 — Final CTA

### Fixed
- **#20 / #22 Section background image removed**: the full-width `web-app-cta-bg.png` (1920×640, ~1.5 MB) at `position: absolute; bottom: 0` is gone. Section now has no background shine — Figma 64:247 is on the page-bg directly.
- **#21 64px CTA button at bottom**: card button moved from `bottom: 88px` to `bottom: 32px` (matches Figma 64:259/64:278 `y=566.5` within a 660-tall card → 660 - 566.5 - 64 ≈ 29.5px from bottom; rounded to 32px). Button itself uses the shared `CtaButton` so `h-16` is now consistent across all CTAs.
- **#23 Phone in left card 168/394**: replaced the wrong `laptop-app-image-01.png` with `mobile-app-image-02.png` at `left: 394px, top: 168px; width: 360px; height: 772px` (overflow clipped by the 710×660 card). Matches Figma 64:262 metadata exactly.
- **#24 Tablet right card pinned right**: replaced the previous phone + tablet pair with just the tablet (`laptop-app-image-02.png`) at `left: 315px, top: 173px; width: 779px; height: 545px`. Matches Figma 64:281 metadata exactly.

### Side cleanups
- `CtaCard` simplified: no longer takes `phoneSrc`/`tabletSrc` props — accepts a single `device` ReactNode for the absolute device wrap (cleaner since the two cards now have very different device specs).

---

## Cross-block — token discipline

### Fixed (alongside the feedback work)
- `tailwind.config.ts` extended:
  - `borderWidth: { '3': '3px', '8': '8px' }` to replace `border-[3px]` / `border-[8px]` arbitrary values used in Block05/Block09/Block10/Block11.
  - `spacing: { 'section-y': '120px', 'content-edge': '240px' }` for the section vertical padding and the canvas-to-content gutter (used by Block01 nav and Block12).
- `Block01` nav: `px-[240px]` → `px-content-edge`.
- `Block02`/`Block03`/`Block04`/`Block05`/`Block08`/`Block12`: removed redundant `leading-[X]` and `tracking-[-0.02em]` utilities where the value was already baked into the `text-text-*` / `text-h*` token. No visual change — just smaller class strings.
- `Block12` body: `py-[120px]` → `py-section-y`.

---

## Build + verification

- `npm run build`: clean. 0 TypeScript errors, 0 Vite warnings.
- Bundle: 189 kB JS gzipped to 54 kB (under 250 kB target). CSS: 18 kB → 4.4 kB gzipped.
- DOM verification at 1440×900 via `preview_inspect`/`preview_eval`:
  - All 5 `CtaButton` instances have `height: 64px`.
  - Block01 hero illustration is anchored top-right with `width: 754px` (~50% of 1440 viewport).
  - Block04 connector image is gone (`querySelector('img[src*="ill-image-one-platform"]')` returns null).
  - Block05 section is 1080px tall.
  - Block07 web-app-cta image is 1055 px wide.
  - Block08 map is 742 px wide.
  - Block12 background image is gone (`querySelector('img[src*="web-app-cta-bg"]')` returns null).
  - Block12 phone in left card at `left: 394px, top: 168px`; tablet in right card at `left: 315px, top: 173px`.
  - Block04 left card uses `mobile-app-sbs.png` at `right: 0, bottom: 0`; right card uses `iphone-bezel + mobile-app-image-01` at `left: 480px, top: 110px`.
  - LogoCard hover state removed (no `.hover\:border-white` selectors in DOM).
  - Block02 H2 has `padding-bottom: 8px` (descender clearance).

### Notes
- `preview_screenshot` timed out repeatedly (likely a tooling issue with the 12 000 px tall page; HMR + render were both healthy). All structural and dimensional checks were verified via DOM inspection instead.
- Figma MCP was inconsistent this session — the original `34:497` root node returned "invalid" mid-session after one successful screenshot. The feedback's specific node IDs (4357, 4317, 4216, 4139, 290, 247) resolved successfully and were used as ground truth where applicable.
